# This repository tracks ONLY the Next.js app in web/.
# Everything else in this directory is local reference material and is never
# committed: source/ (the original prototype), the planning docs, the source
# zip, editor/tooling config, etc.
/*
!/.gitignore
!/web/
